import { Box, Text } from '@chakra-ui/react';
import { useTranslation } from 'react-i18next';

const MemberItem = () => {
  const { t } = useTranslation();
  return <Box>{/* <Text>{t('')}</Text> */}</Box>;
};

export default MemberItem;
